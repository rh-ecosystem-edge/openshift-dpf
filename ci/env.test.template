# OpenShift DPF E2E Test Environment
# Generated by: make generate-env-test
# Do not edit manually — export env vars in your shell and re-run make generate-env-test

# Management cluster access
KUBECONFIG=${KUBECONFIG}

# HyperShift / hosted cluster identification
HOSTED_CLUSTER_NAME=${HOSTED_CLUSTER_NAME}
CLUSTERS_NAMESPACE=${CLUSTERS_NAMESPACE}

# DPF deployment identification
DPF_NAMESPACE=${DPF_NAMESPACE}
DPU_CLUSTER_NAME=${DPU_CLUSTER_NAME}
DPU_DEPLOYMENT_NAME=${DPU_DEPLOYMENT_NAME}

# Sanity / connectivity test tuning
SANITY_TESTS_WORKLOAD_NAMESPACE=${SANITY_TESTS_WORKLOAD_NAMESPACE}
SANITY_TESTS_PING_COUNT=${SANITY_TESTS_PING_COUNT}
SANITY_TESTS_PING_HBN_TO_HBN_PODS=${SANITY_TESTS_PING_HBN_TO_HBN_PODS}

# Node counts
WORKER_COUNT=${WORKER_COUNT}

# Hosted cluster upgrade test (empty = skip)
UPGRADE_RELEASE_IMAGE=${UPGRADE_RELEASE_IMAGE}

# BFB update test — TC-DPUD-002 (all must be set to run, empty = skip)
NEW_BFB_URL=${NEW_BFB_URL}
NEW_BFB_FILENAME=${NEW_BFB_FILENAME}
NEW_BFB_BSP=${NEW_BFB_BSP}
NEW_BFB_DOCA=${NEW_BFB_DOCA}
NEW_BFB_UEFI=${NEW_BFB_UEFI}
NEW_BFB_ATF=${NEW_BFB_ATF}
